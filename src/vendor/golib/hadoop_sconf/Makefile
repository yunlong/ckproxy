all: 
	go test

prepare:
	@sudo -u cloud mkdir  -p /home/s/safe/hadoop_sconf
	@sudo -u cloud scp *.go  /home/s/safe/hadoop_sconf
	@sudo -u cloud scp *.a  /home/s/safe/hadoop_sconf
	@sudo -u cloud scp *.h  /home/s/safe/hadoop_sconf

